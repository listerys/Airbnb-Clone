/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['via.placeholder.com', 'i.postimg.cc', 'ideogram.ai'], // Add other domains as needed
  },
}

module.exports = nextConfig
